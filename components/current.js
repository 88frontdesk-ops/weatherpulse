const current=()=>{document.querySelector(".current_accufeel_shade").style.display=uvIndex>0?"inline-block":"none",document.getElementById("current_condition_table").textContent=window.wCast?.currentWeather?.description||getWeDescription(condition),document.getElementById("title_current_text").textContent=citys.length>20?citys.slice(0,17)+"...":citys,document.querySelectorAll(".current_uv").forEach(item=>{item.textContent=uvIndex}),document.getElementById("current_uv_note").textContent=" "+getUvNote(uvIndex,daylight),document.getElementById("current_cloud").textContent=cloudCover+"%",chrome.storage.local.get(["pressureUnit","windUnit","windDirUnit","visibilityUnit","humidityUnit","setSettingFC"],data=>{let humidityValue;switch(data.humidityUnit){case"gm3":humidityValue=`${GetAbsoluteHumiGm3(humidity,temperature).toFixed(1)} gm³`;break;case"grft":humidityValue=`${GetAbsoluteHumiGrft(humidity,temperature).toFixed(1)} gr/ft³`;break;default:humidityValue=`${humidity}%`}switch(dewPointValue="c"===data.setSettingFC?k2c(dewPoint)+"°C":k2f(dewPoint)+"°F",document.querySelectorAll(".current_humidity").forEach(item=>{item.textContent=humidityValue+" ("+dewPointValue+")"}),currentVisibility.textContent="mi"==data.visibilityUnit?toMi(visibility):toKm(visibility),document.getElementById("current_pressure_trend").textContent="("+pressureTrendIcon(pressureTrend)+")",data.pressureUnit){case"mb":currentPressure.textContent=toMb(pressure);break;case"psi":currentPressure.textContent=toPSI(pressure);break;case"inhg":currentPressure.textContent=toInHg(pressure);break;case"mmhg":currentPressure.textContent=toMmHg(pressure);break;case"kpa":currentPressure.textContent=toKPa(pressure);break;default:currentPressure.textContent=toHPa(pressure)}switch(data.windUnit){case"kmh":currentWind=toKMH(windSpeed),currentWindGust=toKMH(windGust);break;case"ms":currentWind=toMS(windSpeed),currentWindGust=toMS(windGust);break;case"bft":currentWind=toBeaufort(windSpeed),currentWindGust=toBeaufort(windGust);break;case"kn":currentWind=toKnots(windSpeed),currentWindGust=toKnots(windGust);break;case"fts":currentWind=toFtS(windSpeed),currentWindGust=toFtS(windGust);break;default:currentWind=toMPH(windSpeed),currentWindGust=toMPH(windGust)}document.getElementById("current_wind").textContent=currentWind,document.getElementById("current_windGust").textContent=currentWindGust,document.getElementById("current_windBearing_windCompass").textContent="degrees"===data.windDirUnit?toDeg(windDirection):toCom(windDirection)})};